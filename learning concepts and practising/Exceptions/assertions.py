''' Assertions '''

''' an assertion keyword is used when debugging code.
an assertion keyword let's you test if a condition returns true, if not, the program will raise an assertion error 
'''

x = 'raju'
assert x == 'raju'

y = 10
assert y == 5, ' y is not 5'  # we can also print message in assertion error like this